- name: String Variable Demo  # Playbook Name
  hosts: local # run in localhost
  connection: local

  vars: # String Variables
    NAME: Siva Ganesh Thota SL

  tasks: 
  - name: Print Name Vars
    ansible.builtin.debug:   # this is used for to print the msg
      msg: "Hello My Name is {{ NAME }}"
      

