# Instead of assigning variables to each host, assign them to a group.

- name: Group variables Demo
  hosts: web


  tasks:
  - name: Display Group vars
    ansible.builtin.debug:
      msg: "Install {{ PACKAGE }} On Port {{ PORT }}"