---
# Variable Practice Task 1
# Create a Variable

- name: VAr creation
  hosts: local
  connection: local

  tasks:
  vars:
    CITY: Bangalore

  - name: display the city 
    ansible.builtin.debug:
      msg: "i live in {{ CITY }}"

  

