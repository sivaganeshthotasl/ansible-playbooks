- name: installing nginx package
  hosts: local
  connection: local

  vars:
    PACKAGE: nginx

  tasks: 
  - name: Installing Nginx
    ansible.builtin.debug:
      msg: Installing {{ PACKAGE }}




