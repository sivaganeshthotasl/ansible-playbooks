---
# Variable Practice Task 1
# Create a Variable

- name: Variable Creation
  hosts: local
  connection: local

  vars:
    CITY: HYD

  tasks:
  vars:
    CITY: Karnataka
  - name: display the CITY
    ansible.builtin.debug:
      msg: "I live in {{ CITY }}"

  

  

