# Number Comparision

- name: Check Memory Values
  hosts: local
  connection: local

  vars:
    MEMORY: "4"

  tasks:
  - name: High Memory Server
    ansible.builtin.debug:
      msg: "Server has enough Memory"
    when: MEMORY | int >= 6
