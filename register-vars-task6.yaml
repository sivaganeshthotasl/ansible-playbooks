- name: Register Varaibles  # this is name of playbook
  hosts: local  # runs in the localhost
  connection: local

  tasks:

  - name: Check uptime
    command: uptime
    register: UPTIME_OUTPUT

  - name: Print uptime
    debug:
      var: UPTIME_OUTPUT.stdout


