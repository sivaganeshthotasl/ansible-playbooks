# File creation using loop

- name: file creation
  hosts: local
  connection: local
  become: true

  tasks:
  - name: Create files
    ansible.builtin.file:
      path: /opt/{{ item }}
      state: touch
    loop:
      - file1.txt
      - file2.txt
      - file3.txt
