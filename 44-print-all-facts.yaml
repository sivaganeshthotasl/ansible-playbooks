# View All Facts in a playbook
- name: All Facts
  hosts: local
  connection: local

  tasks:
  - name: Display All Facts
    ansible.builtin.debug:
      var: ansible_facts     # This prints all collected facts. 
